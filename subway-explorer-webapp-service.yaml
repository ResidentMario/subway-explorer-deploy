apiVersion: v1
kind: Service
metadata:
  name: subway-explorer-webapp-service
  labels:
    app: subway-explorer-webapp-service
spec:
  ports:
  - port: 3000
    protocol: TCP
  selector:
    app: webapp

